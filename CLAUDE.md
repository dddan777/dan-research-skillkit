This repository ships with repo-local skills under `.claude/skills/`.

- Start with `dan-research` if the user needs stage diagnosis or routing.
- Use the matching `dan-*` topic skill for execution.
- Read the corresponding knowledge pack under `knowledge/packs/` when deeper context is required.
