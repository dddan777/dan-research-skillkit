This repository ships with repo-local skills under `.agents/skills/`.

- Start with `dan-research` if the user needs workflow routing.
- Use a topic-specific `dan-*` skill for concrete academic tasks.
- Read the matching knowledge pack in `knowledge/packs/` when you need the framework or checklist behind the skill.
